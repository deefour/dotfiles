function vm {
  dir=${D4_VAGRANTFILE_HOME:?"The `$DEEFOUR_VAGRANTFILE_HOME` environment variable is not set"}

  if [ ! -f "${dir}/Vagrantfile" ]; then
    echo "`${dir}/Vagrantfile` does not exist"
  fi

  ( cd "$dir" && vagrant "$@" )
}