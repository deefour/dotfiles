function serve {
  local dir=${1:a}

  if [ ! -d "$dir" ]; then
    echo "`$dir` is not a directory"

    return
  fi

  if [ -h "${HOME}/.app" ]; then
    rm -f "${HOME}/.app"
  fi

  ln -s "$dir" "${HOME}/.app" && vm reload
}