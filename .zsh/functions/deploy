deploy () {
  while getopts ":p" opt; do
    case $opt in
      p)
        rake assets:clean && RAILS_ENV=production rake assets:precompile
        git aa && git ci -m "Precompiling asset changes" && git push origin master
    esac
  done

  git push production
}